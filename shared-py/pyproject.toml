[project]
name = "mint-shared"
version = "2.0.0"
requires-python = ">=3.11"
dependencies = [
    "alembic>=1.18.4",
    "sqlalchemy[asyncio]>=2.0.48",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
