APP_PORT= # Port of Auth Service
APP_HOST= # Host of Auth Service -> Defaults to localhost during dev (set it to 0.0.0.0 if using docker)
APP_RELOAD=False # True during development

kEYS_DIR="keys" # RSA keys for JWT

FASTAUTH_SECRET= # FastAuth Secret
